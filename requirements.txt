# Core ML stack
# Install the CUDA/CPU build of torch/torchvision/torchaudio that matches your machine.
torch
torchvision
torchaudio
numpy

# Training/evaluation utilities
tqdm
editdistance
pandas
scikit-learn
matplotlib
hydra-core >= 1.3.2

# Audio/video and preprocessing
opencv-python >= 4.5.5.62
scipy >= 1.3.0
scikit-image >= 0.13.0
av >= 10.0.0
six >= 1.16.0

# Optional inference/cropping backends
mediapipe
